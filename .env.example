# Copy to .env.local

# The Smira API. Leave it unset and the site runs on its own content, which
# is how it ships — nothing here is wired to the backend yet.
NEXT_PUBLIC_API_URL=http://localhost:9100/api

NEXT_PUBLIC_SITE_NAME=Smira Club
NEXT_PUBLIC_SUPPORT_PHONE=+919876543210
